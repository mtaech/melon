/** Absolute path to an asset under the package `assets` dir. */
export declare function assetPath(...segments: string[]): string;
/** Read an asset as UTF-8 text. */
export declare function readAssetText(...segments: string[]): string;
