# dsh-browser-tool bundle layer: register the `browser` tool on the host
# tools registry so every session composed under this profile can call it.
- insert:
    - id: dsh-browser-tool
      name: dsh-browser-tool